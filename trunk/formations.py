import peteship

class Formation:
    pattern = 0
    ships = []
    xys = []
    def __init__(self, shipList, pattern=0):
        self.ships = shipList
        self.pattern = pattern
        xys[0] = (self.ships[0].x, self.ships[0].y)
        

    def calcPattern(self):
        pass
